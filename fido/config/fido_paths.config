# Use this to define your project root
fido_project_root= 

# Primary paths/files based on the root
root_path=${fido_project_root}/fido/
src_dir=${root_path}/src/
build_dir=${fido_project_root}/build/
config_dir=${root_path}/config/

# Executable files
g_align=${build_dir}/Fido
graph_align_job_script=${fido_project_root}/scheduling/graph_align_job.sh

# Supporting files for runtime tracking and computation
orbit_file=${config_dir}/orbit_signatures.txt
time_keeping=${src_dir}/time_results.txt

# Additional tools
kokkos_tools_memory_usage=${fido_project_root}/submodules/kokkos-tools/
#deg_count=${root_path}/parallel_graph_align/node_deg
#static_assign=${root_path}/parallel_graph_align/static_assigner
